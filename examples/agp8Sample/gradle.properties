android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m
# AGP 8 defaults R fields to non-final; Transfuse binds resources via annotations
# (e.g. @Layout(R.layout.main), @View(R.id.greeting)), which require constant R ids.
android.nonFinalResIds=false
