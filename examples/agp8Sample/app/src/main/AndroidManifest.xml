<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name">

        <!-- HelloTransfuseActivity is generated by Transfuse from the HelloTransfuse @Activity POJO.
             Manifest processing is disabled in build.gradle, so the generated activity is declared here. -->
        <activity
            android:name=".HelloTransfuseActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
